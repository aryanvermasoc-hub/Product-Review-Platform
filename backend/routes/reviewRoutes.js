import express from 'express';
import { protect } from '../middleware/authMiddleware.js';

const router = express.Router();

// TODO: Add reviewController imports and map routes here

export default router;